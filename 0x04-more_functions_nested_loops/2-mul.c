#include "main.h"
#include <stdio.h>

/**
 * mul - check the code.
 *
 * @a: input 1
 * @b: input 2
 *
 * Return: Always 0.
 */

int mul(int a, int b)
{
	if (c >= 'A' && c <= 'Z')
	{
		return (1);
	}
	return (0);
}
