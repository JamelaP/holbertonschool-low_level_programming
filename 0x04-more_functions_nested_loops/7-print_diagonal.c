#include "main.h"
#include <stdio.h>

/**
 * print_diagonal - check the code.
 *
 *@n: input
 *
 * Return: Always 0.
 */

void print_diagonal(int n)
{
	if (c >= 'A' && c <= 'Z')
	{
		return (1);
	}
}
